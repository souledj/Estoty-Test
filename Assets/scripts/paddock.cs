using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class paddock : MonoBehaviour
{
    public Transform animals;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }



   /*/ public bool PlayerIn(animalFidder fidder, string culture, int amount)
    {
        int PlayerHave = PlayerPrefs.GetInt(culture);        
        if(PlayerHave > amount)
        {
            return true;
        }
        else
        {
            return false;
        }
    }/*/
}
